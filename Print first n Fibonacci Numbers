public static long[] printFibb(int n) 
    {
        long[] arr = new long[n];
        arr[0]=1;
        if(n==1) return arr;  //to overcome test cases
        arr[1]=1;
        if(n==2) return arr;
        for(int i=2;i<arr.length;i++){
            arr[i]=arr[i-1]+arr[i-2];
        }
        return arr;
    }
